#include <stdio.h>

int main() {
    int n, i;
    char files[10][20];

    printf("Enter number of files: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        printf("Enter file name: ");
        scanf("%s", files[i]);
    }

    printf("\nFiles in Directory:\n");
    for(i = 0; i < n; i++)
        printf("%s\n", files[i]);

    return 0;
}
