#include <stdio.h>
#include <stdlib.h>

int main() {
    int req[] = {98,183,37,122,14};
    int head = 53, i, seek = 0;

    for(i = 0; i < 5; i++) {
        seek += abs(req[i] - head);
        head = req[i];
    }

    printf("Total Seek Time (FCFS): %d\n", seek);
    return 0;
}
